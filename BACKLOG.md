# Backlog

このサイトで「いつかやりたい」アイデアを溜めておく場所。着手したら本文から消すか「✅ 着手済み」を付ける。

---

## 💡 My Books × 人類知マップの統合（読書レイヤー）

**概要**
`playground/my-books/` の読書データを、`playground/knowledge-map/` の歴史地図の上に「読書レイヤー」として重ねて表示する。「人類史の地図の上に、自分が読んだ本が重なる」という、このサイトにしかない体験を作る。

**なぜ面白いか**
- 2つの独立プロジェクトが1つの作品に化ける
- 「自分は人類史のどの時代・地域の本を読んでいるのか」が可視化される
- 読書ログが「記録」から「探索できる地図」に変わる

**実現の土台（既に揃っているもの）**
- `books-data.json` に各書籍の `publishedYear`（原著発行年）が揃っている → 時間軸にプロットできる
- `author` / `themes` / `era`（時代背景）も enrich 済み
- 人類知マップ側は既に「地域 × 西暦」の二軸マップと、カテゴリフィルタ・検索・モーダル詳細のUIを持っている

**考えられる実装アプローチ（要検討）**
- 人類知マップに「📚 読書」カテゴリ（レイヤー）を追加し、`publishedYear` を `year`、原著の地域を `region` としてドットを打つ
  - 地域の推定が課題：`books-data.json` に著者の国／地域情報がない。`authorBio` から推定するか、`region` フィールドを追加して enrich する必要あり
- ドットをクリックすると、通常の詳細モーダルの代わりに My Books の詳細ページ（`book.html`）へ飛ばす、またはモーダル内に書影を表示
- トグルで「読書レイヤーのみ」「歴史のみ」「両方重ねる」を切替

**未解決の論点**
- 現代の本（原著が最近）は時間軸の右端に集中する → warped scale でも潰れる可能性。現代側の解像度をどうするか
- 地域が不明な本（哲学書・翻訳書など）の扱い（`global` レーンに寄せる？）
- My Books のデータスキーマに `region` を足すかどうか（足すなら enrich-book スキルの拡張が必要）

**最小の第一歩（MVPの案）**
まず「原著出版年 × 冊数」の簡易ヒストグラムを My Books 側に出すだけでも、「どの時代の本を読んでいるか」は見える。地図統合はその次のステップ。

---

## その他の未着手アイデア（初回レビューより）

- **My Books の強化**: 一覧の検索・テーマフィルタ、統計ビュー（年別冊数・テーマ分布・原著出版年分布）、詳細ページの前後ナビ・同テーマ回遊
- **Nobel Timeline のデータ駆動化**: 現状ハードコードを JSON 化し、部門フィルタ・日本人受賞者ハイライトを追加
- **ダークモード**: 全ページ共通パレットなので CSS 変数化 + `prefers-color-scheme` で対応可能
- **book.html の SNS 共有対応**: JS でタイトルを書き換えているだけなのでクローラーには全部同じに見える。本ごとの静的 OGP ページ生成を検討
- **共通CSS切り出し**: 各ページにスタイルがコピペされている（ただし「1ファイル完結」は意図的な設計の可能性あり、優先度低）
